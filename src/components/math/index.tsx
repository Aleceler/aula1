export const Multiplicacao = (num1, num2) => {
  return num1 * num2;
};

export const Adicao = (num1, num2) => {
  return num1 + num2;
};

export const Subtracao = (num1, num2) => {
  return num1 - num2;
};
